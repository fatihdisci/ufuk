enum TimeSegment {
  sahur,
  morning,
  noon,
  afternoon,
  sunset,
  night,
}

class SegmentHelper {
  // Mock function for Phase 1
  static TimeSegment getMockSegment() {
    return TimeSegment.morning; // Default for skeleton
  }
}
